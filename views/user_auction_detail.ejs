<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>オークション詳細</title>
    <!-- <link rel="stylesheet" href="/css/bootstrap.min.css"> -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/emp_car_regist.css">
</head>
<body>
    <div id="wrapper">
        <%- include('./emp_header.ejs'); %>
        <div  class="container">
            <div class="row">
                <!-- 左部分 -->
                <div class="col-3">

                    <%- include('./car_left.ejs'); %>
                </div>
                <!-- 右部分 -->
                <div class="col-9">
                    <div class="row">
                        <div class="col-md-auto  w-100">
                            <div class="card mt-3 w-100">
                            <div class="card-body">
                                <h5 class="card-title">オークションネーム：<%= auction.auction_name %></h5>
                                <p class="card-text">開始時間<%= new Date(auction.start_time).toLocaleString() %></p>
                                <p class="card-text">終了時間<%= new Date(auction.end_time).toLocaleString() %></p>
                            </div>
                            </div>
                        </div>
                    </div>
                    <div class="row row-cols-3">
                        <% let index = 0 %>
                        <% for (var car of auction.car_ids) {%>

                            <div class="card  m-1">
                                    <div class="card-body">
                                        <h5 class="card-title">車種名：<%= car.carData.car_name %></h5>
                                        <p class="card-text">グレード：<%= car.carData.grade %></p>
                                        <p class="card-text">カラー：<%= car.carData.outer_color %></p>
                                        <%= car.carData._id %>
                                        <a href="/user/auction/<%= auction._id %>/bit/<%= car.carData._id %>?no=<%= index %>>" type="button" class="btn btn-warning w-60">入札</a>
                                        <% index++ %>
                                    </div>
                                  <hr>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div><!-- .row -->
        </div>
    </div>
</body>
</html>